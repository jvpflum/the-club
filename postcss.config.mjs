// Tailwind v4 is handled by @tailwindcss/vite plugin — no PostCSS plugins needed.
// This file prevents the parent directory's PostCSS config (Tailwind v3) from interfering.
export default { plugins: {} };
